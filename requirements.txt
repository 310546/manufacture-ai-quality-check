# 核心：兼容Python3.12 + Railway容器环境的稳定版依赖
flask==2.3.3
flask-cors==4.0.0
gunicorn==21.2.0
numpy==1.26.4
pillow==10.2.0
# 容器专用OpenCV（无系统依赖，必装）
opencv-python-headless==4.9.0.80
# 可选：如果你的项目用torch，加这行（根据需求删/加）
# torch==2.2.1 --index-url https://download.pytorch.org/whl/cpu
